{
  "name": "edd-jardin-sauvage-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Tests end-to-end (Playwright) de l'application EDD Jardin Sauvage — exécutés en mode démo.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
